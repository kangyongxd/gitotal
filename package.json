{
  "name": "gitotal",
  "version": "0.0.1",
  "description": "`cnpm i -g gitotal` `gitotal --version` `gitotal --help`",
  "bin": {
    "gitotal": "bin/gitotal.js"
  },
  "main": "gitotal.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^3.0.0",
    "commander": "^4.0.1",
    "fs-extra": "^8.1.0",
    "glob": "^7.1.6",
    "simple-git": "^1.126.0"
  }
}
